# Security

This repository contains educational experiments using DES, an obsolete
cipher, over deliberately reduced key spaces.

It must not be used to protect real data or as a production cryptographic
library.

If you find a correctness issue that changes a documented experimental result,
please open a GitHub issue describing the affected algorithm, parameters, and
a minimal reproduction.
